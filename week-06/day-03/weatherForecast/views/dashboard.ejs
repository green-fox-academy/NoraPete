<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Weather forecast</title>
  </head>
  <body>
    <% for (let i = 0; i < database.length; i ++) { %>
      <buton>
        <div class="city">
          <h1><%= database[i].city %></h1>
          <p><%= database[i].location %></p>
        </div>
        <div class="temperature">
         <h2> <%= database[i].weather[0].temp %> °C </h2>
        </div>
        <img src=<%= '/' + database[i].weather[0].icon + '.png' %>>
      </buton>
      <% } %>
  </body>
</html>